server {
  listen 80;
  listen [::]:80;
  server_name gateway;

  location /api {
    rewrite /api/([^/]+) /$1 break;
  }

  location /health-check {
    add_header Content-Type text/plain;
    return 200 "success";
  }
}
