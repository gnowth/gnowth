server {
  listen 80;
  listen [::]:80;
  server_name gateway;

  location /api {
    rewrite /api/([^/]+) /$1 break;
    proxy_pass http://service_stubby:8882;
  }

  location /health-check {
    add_header Content-Type text/plain;
    return 200 "success";
  }
}
