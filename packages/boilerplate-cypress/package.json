{
  "author": "Thierry Wong",
  "description": "Boilerplate for end to end with cypress",
  "license": "AGPL-3.0-only",
  "name": "@boilerplate/cypress",
  "prettier": "@gnowth/prettier-config",
  "private": true,
  "sideEffects": false,
  "version": "0.0.0",
  "eslintConfig": {
    "extends": [
      "@gnowth/eslint-config/hybrid"
    ]
  },
  "repository": {
    "directory": "packages/boilerplate-cypress",
    "type": "git",
    "url": "https://github.com/gnowth/smart.git"
  },
  "dependencies": {
    "cypress": "10.11.0",
    "del": "7.0.0",
    "local-cypress": "1.2.6"
  },
  "devDependencies": {
    "@gnowth/eslint-config": "0.0.0",
    "@gnowth/prettier-config": "0.0.0",
    "@gnowth/typescript-config": "0.0.0"
  },
  "scripts": {
    "build": "cp ../../LICENSE LICENSE",
    "clean:temp": "rm -rf LICENSE cypress/media",
    "lint:script": "eslint --ext mjs,ts .",
    "lint": "npm run lint:script",
    "test:mock": "cypress run --config baseUrl=$CYPRESS_BASEURL --config-file cypress/cypress.config.mock.mjs --headless",
    "test:regression:chrome-mobile": "cypress run --config baseUrl=$CYPRESS_BASEURL,viewportHeight=667,viewportWidth=375 --config-file cypress/cypress.config.regression.mjs --headless --browser chrome",
    "test:regression:chrome": "cypress run --config baseUrl=$CYPRESS_BASEURL --config-file cypress/cypress.config.regression.mjs --headless --browser chrome",
    "test:regression:firefox-mobile": "cypress run --config baseUrl=$CYPRESS_BASEURL,viewportHeight=667,viewportWidth=375 --config-file cypress/cypress.config.regression.mjs --headless --browser firefox",
    "test:regression:firefox": "cypress run --config baseUrl=$CYPRESS_BASEURL --config-file cypress/cypress.config.regression.mjs --headless --browser firefox",
    "test:regression": "cypress run --config baseUrl=$CYPRESS_BASEURL --config-file cypress/cypress.config.regression.mjs --headless",
    "test:smoke": "cypress run --config baseUrl=$CYPRESS_BASEURL --config-file cypress/cypress.config.smoke.mjs --headless",
    "typecheck": "tsc"
  }
}
