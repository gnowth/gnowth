import { breakpoint } from './scale-breakpoint'
import { buttonSize } from './scale-button-size'
import { fontFamily } from './scale-font-family'
import { fontSize } from './scale-font-size'
import { iconSize } from './scale-icon-size'
import { length } from './scale-length'
import { space } from './scale-space'
import { zIndex } from './scale-z-index'

export const scales = {
  breakpoint,
  buttonSize,
  fontFamily,
  fontSize,
  iconSize,
  length,
  space,
  zIndex,
}
